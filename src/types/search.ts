export interface User { id: string; username: string; avatar_url?: string; } export interface Look { look_id: string; user_id: string; username?: string; image_url: string; title?: string; description?: string; tags?: string[]; } export interface Tag { name: string; id?: string; description?: string; } export interface Style { name: string; id?: string; description?: string; } export interface Brand { name: string; id?: string; logo_url?: string; } // Legacy search results structure export interface CategorySearchResults { users: User[]; looks: Look[]; tags: Tag[]; styles: Style[]; brands: Brand[]; total: number; } // New contextual search result item export interface SearchResultItem { id: string; type: 'user' | 'look' | 'wardrobe' | 'style' | 'tag' | 'brand'; primary_text: string; secondary_text?: string; description?: string; image_url?: string; context: string; url: string; } // New contextual search results export interface ContextualSearchResults { results: SearchResultItem[]; total: number; } // Combined type for backward compatibility export type SearchResults = CategorySearchResults | ContextualSearchResults; export type SearchCategory = 'all' | 'users' | 'looks' | 'tags' | 'styles' | 'brands' | 'ai-search'; 