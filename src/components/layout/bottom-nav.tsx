'use client'; import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { Home, Search, Plus, Heart, User } from 'lucide-react'; interface BottomNavProps { className?: string; } export function BottomNav({ className = '' }: BottomNavProps) { const pathname = usePathname(); const isActive = (path: string) => { return pathname === path || pathname.startsWith(`${path}/`); }; return ( <nav className={`fixed bottom-0 left-0 right-0 z-10 bg-white border-t border-gray-200 md:hidden ${className}`}> <div className="flex justify-around items-center py-2"> <Link href="/" className={`flex flex-col items-center p-2 ${ isActive('/') ? 'text-blue-600 ' : 'text-gray-600 ' }`} > <Home size={24} /> <span className="text-xs mt-1">Home</span> </Link> <Link href="/search" className={`flex flex-col items-center p-2 ${ isActive('/search') ? 'text-blue-600 ' : 'text-gray-600 ' }`} > <Search size={24} /> <span className="text-xs mt-1">Search</span> </Link> <Link href="/create" className={`flex flex-col items-center p-2 ${ isActive('/create') ? 'text-blue-600 ' : 'text-gray-600 ' }`} > <Plus size={24} /> <span className="text-xs mt-1">Create</span> </Link> <Link href="/saved" className={`flex flex-col items-center p-2 ${ isActive('/saved') ? 'text-blue-600 ' : 'text-gray-600 ' }`} > <Heart size={24} /> <span className="text-xs mt-1">Saved</span> </Link> <Link href="/profile" className={`flex flex-col items-center p-2 ${ isActive('/profile') ? 'text-blue-600 ' : 'text-gray-600 ' }`} > <User size={24} /> <span className="text-xs mt-1">Profile</span> </Link> </div> </nav> ); } 