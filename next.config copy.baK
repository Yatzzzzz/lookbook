/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  webpack: (config) => {
    config.externals.push({
      'ws': 'commonjs ws',
    });
    return config;
  },
};

module.exports = nextConfig; 